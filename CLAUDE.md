# 滑動勇者 (Daydreaming Swipe)

姊妹作:[bozhan9527-ux/daydreaming-](https://github.com/bozhan9527-ux/daydreaming-)(勇者發呆中,放置型)。
兩款**共用同一套數值資料層與像素美術**,但核心迴圈完全不同,不要把兩邊的設計混在一起想。

## 這款是什麼

**左右滑動的跑道闖關。** 角色自動往前跑,玩家**用手指把勇者拉著左右移動**——位置是連續的,
手指到哪勇者就到哪。跑道只有**兩條**:沒有中立格,每一排都是二選一,一定要表態。
路上一排一排出現閘門(好格三種:勇者 x2(每場固定 2 個的爆發格) / 勇者 +N(固定值) / 裝備強化;壞格是勇者 x0.5 / 裝備損壞 / 勇者 -N)與敵人波次,
**只有勇者當下站的那一格會生效**。核心決策是「下一排我要站哪一格」,而且只有一兩秒可以決定。
**關卡結構**:**300 大關** x 10 小關(1-1 … 1-10)。一般小關 **10 波、30 排、約 3 分鐘**;
小關編號是 5 的倍數的那兩關(x-5、x-10)加倍長——**20 波、40 排、約 6 分鐘**。
**兩種小關的閘門數都是 20 個**:長關是「波數翻倍、閘門不變」,它的硬來自敵人一波接一波。
x-10 同時是魔王關。第一大關的容錯係數由鬆到緊,是新手教學區。
閘門**不佔滿跑道**,左右都留空隙:站對邊還不夠,沒把勇者拉到框上面就整格漏掉(好處沒吃到,
陷阱也沒踩到)。起跑位置剛好就在中間的空隙上——不動的話什麼都吃不到。
**判定是「史萊姆的身體碰到就算」,而且隊伍越多人身體越寬**(見 `game/laneRun.ts` 的
`heroHalfSpan`)——滾出一支隊伍等於同時買到「比較不會漏接」,那是人數的第三個好處。
戰鬥段上另外散著**路障石頭**(每場 2~3 顆,長關按比例加倍):撞到掉 20% 人數。
石頭跟閘門是兩種東西——閘門是**選擇**(一定要挑一邊,挑錯是變弱),石頭是**反應**
(站哪裡都行,只要不是它那裡,撞到是失誤)。

戰力 = **勇者人數 x 每人攻擊力**,兩個乘數缺一不可:只堆人數不換裝備,人再多也打不動後面的怪;
只換裝備不堆人數,一個人再強也擋不住一整波。閘門就是在逼玩家每一排決定補哪一邊。

> 曾經做過一版「定點閃避」(怪物出招 → 往正確方向滑開 → 反擊),後來確認要的是短影音廣告裡
> 那種跑道選閘門的體感,整組換掉了。兩者都叫左右滑,但差別在:閃避的滑動是「反應」,
> 跑道的滑動是「選擇」——選錯不是受傷,是變弱。現行機制只有 `game/laneRun.ts` 這一套。

## 跟姊妹作最關鍵的差異(別搞混)

| | 勇者發呆中 | 滑動勇者(本作) |
|---|---|---|
| 核心賣點 | 不玩也在成長 | 玩得好才打得贏 |
| 玩家做什麼 | 什麼都不用做,開著就好 | 每 1~2 秒把勇者拉到想吃的那一格 |
| 養成 | 裝備/技能/寵物一路疊 | **轉職(第 5/30/80/160/260 大關)**,只調整起跑數值;技能書與圖鑑則只放大場內技能 |
| 場內成長 | 沒有(離線也在長) | **場內技能**:每打完一波三選一,只在這一場有效,跑完就沒 |
| 戰鬥模型 | 連續進度條(`fightElapsedMs / fightDurationMs`) | 一排一排結算,只有你站的那格生效 |
| 生命 | 獨立的血量 | **人數就是血量**:打不掉的怪撞上來換掉勇者,人數歸零就死 |
| 難度來源 | 數值養成 | **跑速 + 閘門好壞差距 + 失誤的複利**(見 `game/laneRun.ts`) |
| 離線收益 | 核心系統 | **不存在**,不要加回來 |
| 關卡長度 | 3000 關 | **300 大關 x 10 小關 = 3000 小關** |

**鐵則:轉職與技能只調整「起跑時的武器階級、戰力與兌換率」,絕對不放慢跑速、不減少陷阱。**
**而且所有職業一律 1 人起跑**——人數只能靠跑道上的閘門滾出來,「從一個人變成一支隊伍」是本作
一場跑圖的弧線,起跑就先給一票人會把它抹平。這條同時也讓「勇者 +N」閘門變成職業中立
(只有一個人的時候每人攻擊力就等於總戰力,同一格對誰都一樣值)。
而且**起跑倍率要小**:閘門全是相對值(x2、裝備強化都是乘的),起跑差幾倍就會一路放大到終點。
實作時踩過一次——讓人數與裝備各自再乘上去,滿階職業起跑就有 7 倍多,第 25 關「亂選」的過關率
從 38% 衝到 85%,養成直接買到勝利。現在的做法是 `RunStart.attackMultiplier` 乘的是**總戰力**,
人數與武器階級只決定這份總戰力怎麼分配(見 `game/laneJobs.ts` 的註解)。
技能因為**每關都給**,幅度壓得比轉職更小:3 組 x 5 級全部點在戰力上,加起來也只有 +45%,
而且技能之間是**相加不是相乘**(相乘算不出上限)。`verify-lane-skills.ts` 會盯著這個上限。

**場內技能(`game/laneRunSkills.ts`)是另一回事,不受這條鐵則管。** 它每打完一波給一次
(最後一波之前多給一次),只在**這一場**有效,跑完歸零,所以幅度可以大一個量級
(每級 +18%~+30%,一般小關 5 次、長關 10 次,一場能疊到 2.4~4.3 倍)。
不會「養成買到勝利」的原因是它根本不累積——下一場一樣從零開始。這是 roguelite 的迴圈,
跟永久養成的迴圈並存,但**不能混進同一個系統**,不然幅度只能遷就比較嚴的那一邊。

## 目前進度

- [x] 資料層搬遷(裝備 3000 件、怪物 120 種、寵物坐騎、技能樹、成就、材料)
- [x] 美術資產搬遷(`assets/sprites` 1500+ 檔)
- [x] 跑道闖關核心 `game/laneRun.ts`(數值驗證全通過)
- [x] 跑道畫面 + 拖曳操作(連續位置;方向鍵是給桌機測試用的,會吸附到跑道中央)
- [x] 閘門判定改成**身體碰到就算**,寬度跟著隊伍人數長;跑道上的反白/分隔線/虛線/判定線全部拿掉
- [x] 勇者與敵人改用 `assets/sprites` 的既有像素圖
- [x] 戰鬥演出:一波小怪連續衝過來、勇者不斷擲出武器,打得掉幾隻由攻擊力決定
- [x] 人數的爽感:隊伍最多畫 14 個人(各自晃動),投擲密度隨人數上升(實測 1 人 0.8 把 → 11 人 2.8 把)
- [x] 一波混 3 種怪、各自偏離跑道中心;**每個大關一張場景底圖**(1-1…1-10 同一張,
      10 張用完循環;見 `game/laneRun.ts` 的 `backdropForStage`)
- [x] 轉職:第 5/30/80/160/260 大關各一次,6 路線 x A/B 分支 x 5 階;給的是**起跑數值**
- [x] ~~永久技能(鋒鑄/強化/堅韌/工藝)~~ **已整組移除**(存檔 v5 → v6 把欄位丟掉)。
      養成現在只剩三條:轉職、技能書、圖鑑——**其中只有轉職進理想路線**。
- [x] 場內技能:一場 **10 格**、每打完一波三選一;**六元素 x 三階 = 18 款**,跑完歸零。
      **湊滿同一個元素的三階 → 那一族的主動傷害 +50%**(`ELEMENT_SET_BONUS`);
      10 格剛好放得下三族(9 格)+1,所以最多專精三個元素,仍然要放棄另外三個。
      一階被動、**二三階主動**(冷卻好了而且畫面上有怪就自己放),二三階全部造成傷害
      (只有木一階例外,它是補人)。二階要先有一階、三階要先有二階。
      被動 2 款(鋒刃/增殖)與舊的主動 4 款(爆裂/貫穿/號令/壁障)都已依指示移除——
      **所以現在沒有任何技能會加戰力**,場內技能對理想路線的貢獻是 0,
      敵人一格都不會為技能變強(`maxRunSkillAttackMultiplier() === 1`,verify 在盯)。
      格數必須少於款數(4 格 / 6 款),不然「要不要拿新的」就不是決策;
      而且**一場的選擇次數不能超過 4 格 x 5 級 = 20**,超過的話最後幾次會開出空面板
      (長關剛好 20 次,一格餘裕都沒有,verify 有兩項在盯)。
- [x] 六元素相剋:**單一閉環**(金→木→土→冰→火→雷→金),**逐元素**放大/削弱
- [x] 移除光(護盾)與暗(吸取);雷原本在第二個環上,環斷了會變孤兒,所以併進主環
- [x] 土改成**減緩怪物前進**(咖啡色)
- [x] 屬性長在怪身上:一波共用一個屬性、整群染成那個顏色;關卡前公開整關的屬性順序
- [x] **移除主動技能**(爆裂/貫穿/號令/壁障)。轉職的階梯(1~5 款)留著沒拆,
      主動要加回來直接接得上;現在轉職的獎勵只剩起跑數值(`runStartFor`)
- [x] 挑技能時**跑圖暫停**(試過不暫停的版本,翻回來了——見下方的坑)
- [x] 技能列:畫面最下方一列,顯示帶著的技能與等級(主動移除後沒有冷卻倒數了)
- [x] 火/雷/冰改成**命中當下**發生:燃燒擴散 / 每 N 下連鎖閃電 / 機率凍在原地,
      連鎖電到的目標再各自吃一次擴散與凍結(演出參數與擊殺數是兩組數字)
- [x] 波型:小怪波 / 中點精英(一隻抵 6 人)/ 勇者波(投擲武器要閃)/ x-10 魔王
- [x] 吸收:打倒的怪有一部分加入隊伍(全清一定至少 +1),打完一波終於有正向回饋
- [x] 前方沒東西就加速趕路(閘門進視野絕不加速,反應時間一秒都不偷)
- [x] 大魔王:每 10 關一場,放在整場的最後一排(造型沿用姊妹作的 5 款關卡魔王 + 大魔王本尊)
- [x] 廣告版位:畫面最上方的固定高度佔位(還沒接聯播網,見 `components/AdSlot.tsx`)
- [x] 難度曲線重做:敵人綁定閘門成長,一關 20 排,張力往後遞增而不是往後遞減
- [x] 第 40 小關之後的難度分段:一段轉一顆旋鈕(閘門變窄 → 陷阱變重 → 勇者波變密)
- [x] 部署到 GitHub Pages(https://gotbemate-del.github.io/daydreaming-swipe/)
- [x] 主介面:勇者站中間 + 開始闖關,每場結束都回到這裡(過關才前進、失敗重打同一關)
- [x] 打擊演出:命中跳傷害數字、22% 機率暴擊(金色、2 倍數字)——純演出,不影響擊殺數
- [x] 路障石頭:戰鬥段每場 2~3 顆(長關加倍),撞到掉 20% 人數、撞不死人;
      **不進理想路線**,所以完全沒動到難度曲線與結構保證(見下方「石頭」那條)
- [x] 敵人兩格動畫(小怪/精英/魔王/勇者波):`_open` + `_middle`,建置時對齊重心與底邊
- [x] 勇者波的投擲動作:職業立繪多一格 `_click`,出手的那一瞬間才換過去
- [x] 存檔:關卡、職業、永久技能、金幣寫進 AsyncStorage;壞掉/改過/舊版存檔都進得去
- [x] 生存模式(副本分頁):**一條不中斷的跑圖**,通關不停下來等按鈕、直接接下一關;
      分數是**累計波數**,死了就結束,撐過幾波換技能書
- [x] 技能書:**上限 100 級**,只放大元素與主動的效果(**不碰選項串**,見下方的坑)。
      曲線是冪次(`bookBonus`):1 級 +6% / 10 級 +30% / 50 級 +92% / 100 級 +150%。
      **每通一關保證給 3 本**(長關 6 本;圖鑑的掉落率變成「再多一本」的機率),
      三十幾關練滿一輪。
      存檔 v4 → v5 把舊的 0~5 級換算成倍率相同的新等級(5 級 → 37 級)。
- [x] 裝備圖鑑(裝備分頁):**501 個條目**(= 501 張美術圖),5668 件裝備當碎片;
      每件顯示圖示/名稱/碎片 n/m/等級/該屬性加成,六屬性各自累積、各自封頂 +100%
- [x] 背景音樂:既有的 `assets/sounds/bgm.wav` 循環播放;**音效**接上既有的短音
      (按鈕/通關/陣亡/選技能/抽地圖)
- [x] 設定齒輪:主介面右上角與**跑圖中**各一顆。跑圖中打開就是**暫停**;
      面板有音樂開關 + 音樂音量 + 音效音量兩條滑桿(全部存進存檔)
- [x] 生存模式的地圖**用抽的**:開跑前從 10 張裡抽一張,整輪不變;
      開頭的面板可以重抽一次,3 秒後自動開始
- [x] 教學關卡 **1-1 ~ 1-5**:逐關**開放機制**(不是放水),一關只多學一兩件事;
      波數也跟著縮(5/6/8/10,1-5 照通則 20 波),見 `game/laneTutorial.ts`
- [x] 遊戲任務:引導玩家去點介面上那些跑道教不到的東西(設定/圖鑑/三個副本/永久技能/轉職);
      主介面一條橫幅 + 一頁清單,獎勵一律金幣(`game/quests.ts`)
- [x] 副本拆成三種:**無限副本**(原生存模式)/ **技能書副本**(通關必得 3 本,但不推進進度)/
      **裝備副本**(通關掉一整批碎片)。三種共用同一條敵人曲線,差別只在規則與獎勵;
      兩個單場副本收金幣入場費(`game/dungeons.ts`)——那是金幣的第一個去處。
      **兩個單場副本每天固定開一個屬性**(照相剋環六天一輪)、**各自每天最多通關 5 次**,
      通關拿的是那個屬性的技能書/裝備碎片
- [x] 技能書改成**逐屬性**(六個元素各自 0~100 級),跟圖鑑同一條軸:
      火的書只放大火系三階。單一等級的話「今天開哪個屬性」就只是門票顏色
- [ ] 下方分頁列剩下八個功能仍然未開放(只畫出來並鎖住,見下方「裝備為什麼還沒接」)
- [x] `game/equipment.ts` 的 5668 件接上了,但**是當圖鑑不是當裝備**(見下)。
      跑圖裡的「裝備」仍然只是 1~5 階的抽象等級,那是兩件事。

### 裝備為什麼是「圖鑑」而不是「穿在身上」(先讀這段再動手)

`game/equipment.ts` 實際上是 **5668 件**(不是 3000),但它整份是為姊妹作的掛機迴圈設計的,
不能直接接上來,三個硬衝突:

1. **加成只有 `speed` / `exp` / `coins` 三種。** `speed` 在姊妹作是「縮短戰鬥時間」,
   在這款對應的是跑速——而跑速是難度旋鈕,鐵則明文禁止養成碰它。接上去等於「花錢買慢速」。
2. **`exp` 沒有對應物。** 這款不打算做經驗值系統。
3. **5650/5668 件有 `requiredLevel`(最高 500)。** 沒有等級,這個門檻整個失效,
   要換成別的(金幣價格是現成的,`price` 0~16730,而跑圖本來就產金幣)。

而且滿裝加總是 speed +144%,是技能上限(+45%)的三倍多——直接接上去就是養成買到勝利。
**解法是不要用那三個加成欄位。** 改成圖鑑之後三個衝突同時消失:圖鑑只在乎「這件有沒有收到」,
`speed`/`exp`/`requiredLevel` 一個都不用讀,那 5668 件也從「數值棒」變成「內容」。

圖鑑給兩樣東西,都刻意在理想路線之外(見 `game/collection.ts`):

- **屬性加成**:放大元素與主動的效果(跟技能書同一條軸)
- **技能書掉落率**:2% → 12%,完全不進戰鬥

**一格 = 一張美術圖,底下的裝備是它的碎片。** `assets/sprites/items` 有 **501 張**圖,
而裝備有 5668 件——同一個(職業, 階級, 分支, 部位)的所有 bracket 共用一張,每張背後 3~34 件。
這件事本來是限制(「沒辦法一件一張」),翻過來看剛好就是碎片系統:收集底下的裝備 = 湊那一件的碎片,
湊得越多等級越高(1~5 級),每一級給**它綁的那一個屬性** +1%,單一屬性封頂 +100%。
好處是它完全不必發明新的儲存格式——碎片就是既有的 5668-bit bitset,**存檔一個欄位都不用改**。
逐屬性而不是一條總收集率,是為了讓蒐集有方向感(「我還差雷的那幾件」)。
圖示走 `scripts/shrink-items.py` 產生的 112px 複本(原圖 49MB → 6.6MB),**原圖不動**——
跑圖裡投擲的武器仍然吃原圖。

**絕對不要讓圖鑑加戰力或加人數。** 那兩個是貪心(`bestRunSkillChoice`)看得到的,
敵人會立刻跟著長,玩家蒐集半天卻感覺不到差別。`verify-lane-skills` 有一項在盯這件事。

真的要做「穿在身上」的裝備,那仍然是一次獨立的設計決策,不是接線工作。
- [x] 關卡結構:大關 x 10 小關,5 的倍數加倍長,x-10 是魔王關;第一大關放寬當教學區
- [ ] 經驗曲線重新校準(原本是為掛機設計的)——**目前不做,但保留這個可能性**。
      現階段跑圖沒有經驗值,養成只有技能與轉職;未來要加回來的話,姊妹作那條曲線不能直接用。
- [x] 存檔 schema v8(`game/save.ts` + `hooks/useSave.ts`,全新,不沿用姊妹作的 v36)

## 專案結構

```
app/          Expo Router 頁面層(只做組裝)
components/   UI 元件
  MainMenu.tsx    主介面(勇者站中間 + 開始闖關 + 下方分頁列 + 右上角設定)
  Settings.tsx    設定面板(音樂開關 + 兩條音量滑桿;跑圖中打開 = 暫停,並多兩顆「重新再來 / 放棄遊戲」)
  VolumeSlider.tsx 音量滑桿(手寫,20 格)
  MapDrawToast.tsx 生存模式開頭的抽地圖面板(可重抽一次)
  LaneRunner.tsx  跑道畫面(拖曳、閘門、波次、投擲演出、傷害數字、場內技能面板)
  Quests.tsx      任務面板(一頁清單 + 領獎;主介面橫幅點下去就是這裡)
  DungeonSelect.tsx 副本選擇(三張卡片:無限 / 技能書 / 裝備,含解鎖條件與入場費)
  JobChoice.tsx   轉職畫面
  AdSlot.tsx      廣告版位(先佔高度,之後接 AdSense/AdMob 只改這個檔)
  artAssets.ts    既有素材(PNG)的 require 對應表 ← game/ 不能碰圖片資源,所以放這層
  animFrames.ts   **產生檔**(scripts/align-frames.py),敵人的兩格動畫 + 畫框比例,不要手改
  stageBackdrops.ts **產生檔**(scripts/shrink-backdrops.py),大關底圖 + 底色/線色/暗紗濃度
game/         遊戲核心邏輯(純函式,禁止 import React)
  laneRun.ts      跑道/閘門/敵人結算 ← 本作的心臟
  laneTutorial.ts 教學關 1-1~1-5:哪一關開放哪些機制、幾波、容錯係數、教學文案
  quests.ts       任務:定義與進度判定(達成與否**現算**,存檔只存「領過獎沒」與事件計數器)
  dungeons.ts     三種副本的規則、解鎖時機、入場費、獎勵倍率
  laneJobs.ts     轉職:選項、起跑數值
  laneRunSkills.ts 場內技能:選項、等級、當場加成(每波一次,跑完歸零;主動技能的秒冷卻也在這)
  save.ts         存檔格式、驗證、遷移(**只有格式,讀寫在 hooks/useSave.ts**)
  collection.ts   圖鑑的收集狀態(5668-bit bitset)與它給的加成
  codexEntries.ts 501 個圖鑑條目(= 501 張圖),以及「哪幾件裝備是它的碎片」
  equipment.ts    3000 件裝備(自姊妹作搬入,未改)
  monsters.ts     120 種怪物(自姊妹作搬入,未改)
hooks/        自訂 hooks
  useSave.ts      存檔讀寫(AsyncStorage;web 上就是 localStorage)
  useBgm.ts       背景音樂(**掛在 app 層,不能掛進 LaneRunner**,見下方的坑)
  useSfx.ts       音效(模組層單例 `playSfx(id)`,音量由 app 層同步進去)
  useNoContextMenu.ts 網頁版關掉右鍵選單與長按選單(同樣**掛在 app 層**,理由同上)
assets/       圖片、音效
scripts/      驗證腳本
  simRun.ts       跑一場的模擬器 ← 三份 verify 共用,不要各自複製一份迴圈
  verify-save.ts  存檔驗證(壞掉的存檔、改過的存檔、舊存檔遷移)
  verify-lane-tutorial.ts 教學關驗證(機制階梯、結構保證、教學區以外沒被動到)
  align-frames.py 把 _open/_middle 對齊成兩格動畫,並產生 components/animFrames.ts
  shrink-items.py 把 501 張裝備原圖縮成圖鑑用的 112px 小圖,並產生 components/itemIcons.ts
  shrink-backdrops.py 把 10 張大關底圖縮成 768px,並算出各自的底色/線色/暗紗濃度
```

**分層鐵律**:`game/` 是純邏輯層,必須能在 Node 單獨跑。任何 React/RN/Expo 的 import
出現在 `game/` 內都算錯誤。

## 驗證流程(每次修改後必跑,順序固定)

1. `npx tsc --noEmit` → 必須零錯誤
2. 改動 `game/` 內任何檔案 → 跑對應的驗證腳本
   (`verify-lane-run.ts`、`verify-lane-jobs.ts`、`verify-lane-skills.ts`,必須全部 PASS)
3. 改動畫面 → Playwright 實機測試 + 截圖,並用 Read 工具實際看過截圖
4. 改動存檔結構 → 附遷移邏輯(`game/save.ts` 的 `MIGRATIONS`),並跑 `verify-save.ts`
   (裡面有一項就是模擬「舊存檔載入」)

## 回報問題時的處理方式(重要)

使用者回報「壞掉了/卡住了」的時候,**先問清楚再動手**,不要直接開始重現。至少要確認:

1. **在哪個畫面**?(跑圖中 / 通關結果 / 技能 / 轉職)
2. **做了什麼動作**?(按了哪顆按鈕、拖了沒反應、還是畫面完全不動)
3. **什麼裝置**?(手機還是電腦、螢幕大小)——版面類的 bug 幾乎都跟這個有關

這次「跑完第一關卡住」花了很久才定位,就是因為我一開始猜成「拖曳失效」去重現觸控,
實際上是「小螢幕把按鈕切到畫面外」。一句「你是手機還是電腦、按下去有沒有反應」就能省下那段。

## 踩過的坑(別再踩)

**版面**
- 跑道高度曾經寫死 500px,只在 780px 高的視窗測過 → iPhone SE(667)、小 Android(640)
  以及被瀏覽器網址列吃掉高度的手機,最底下那一列(「下一關」按鈕)被切在畫面外,
  畫面又不能捲動,玩家直接卡死。**版面只要動過,就要跨 640/667/780/844 四種高度量一次。**
- 立繪共用同一個框 + `resizeMode contain`,框比圖窄的話寬的那幾張會被縮矮
  (職業圖 458 寬、學生 353 寬)。框一律取最寬的那張。
- **無限捲動的底圖要往下鋪,不是往上鋪,而且份數不能寫死。** 第一版是
  `top: shift - height * k`(k 越大越往上),所以不管疊幾份,最下面永遠停在 `shift + height`——
  跑道底部固定露出一條純底色的黑帶,而**勇者剛好就站在那一段**。
  正方形的圖(378px 配 500px 高的跑道)必中,長條的圖看不出來,所以只測一張會漏掉。
  現在是把第一份推到畫面上緣之外(`shift - height`)再一路往下鋪,
  份數 `ceil(跑道高 / 圖高) + 1` 跟著跑道高度走,**不隨跑的距離長**。

**存檔**
- **音量是小數,不能拿讀整數的那支函式去讀。** `readInt` 會 `Math.floor(0.35)` = 0,
  症狀是「玩家調好音量,重開之後遊戲完全沒聲音」——而且看起來像音效功能壞了,
  不像存檔壞了。而**壞掉的值要退回預設值不是退回 0**,理由同一個。`verify-save` 兩項都在盯。
- **讀存檔是非同步的,讀完之前一個字都不能畫遊戲。** 沒有 `loaded` 旗標的話,玩家會先看到
  1-1 的主介面閃一下才跳回真正的進度——而如果他剛好在那一瞬間按下「開始闖關」,
  這一場結束時就會把 stage 1 寫回去,**300 關的進度被一個 loading 閃爍蓋掉**。
- **存檔內容一律當成來路不明的 JSON。** 它在 web 上就是 localStorage,玩家改得動。
  每個欄位各自驗證、各自夾回合法範圍,**壞掉的欄位只丟那一個**——整份丟掉的話,
  一個手滑的 coins 會讓玩家失去整條進度。而且驗證失敗絕對不能丟例外:
  症狀要是「回到預設值」,不是白畫面(白畫面等於玩家再也進不去,連清存檔的入口都沒有)。
- **只存跨場留下來的東西。** 跑到一半的那一場、正在挑的技能、正在轉職的畫面都不存——
  存了就會有「復原到一半的一場」這種永遠測不完的狀態。代價是挑技能時關掉分頁要重打那一關,
  但金幣已經進帳(`onRunFinish` 先加),不是整場白跑。

**音效**
- **背景音樂的播放器不能掛在 `LaneRunner` 裡。** 那個元件每一關都換 key 重新掛載
  (生存模式是一關接一關),掛在裡面音樂會每十波從頭播一次。掛在 `app/index.tsx`
  才跨得過主介面 / 跑圖 / 圖鑑 / 轉職。
- **`expo-audio` 的 web 版 `play()` 不回傳 promise**(`AudioPlayerWeb.play()` 裡面是
  `this.media.play()`,沒有 return),所以**呼叫端接不到自動播放被擋的那個錯**——
  它會變成一行 unhandled rejection 紅字(`play() failed because the user didn't
  interact with the document first`)。解法不是「呼叫了再接錯」,是**在網頁上根本不要
  在互動之前呼叫**:掛一次性的 pointerdown/keydown,玩家第一次碰畫面才開播。
  那個「有沒有互動過」的旗標要放模組層,中途關掉音樂再打開時才會立刻出聲。
- **音樂開關一定要存進存檔。** 關掉之後每次重開又自己播起來,比沒有音樂還糟。
  它是唯一一個不是「進度」的存檔欄位,而壞掉的值要退回**開著**(那是預設的體驗)。

**操作**
- **網頁版一定要把右鍵選單與長按選單關掉。** 這款唯一的操作是「按著拖」而且一排只有一兩秒,
  瀏覽器的兩個預設行為剛好都在那個時候把手打掉:桌機右鍵跳系統選單、手機長按跳
  「複製 / 分享 / 搜尋圖片」——而跑道整片都是 `<img>`(底圖、勇者、怪),長按幾乎必中。
  選單蓋住的正是前方那一排閘門,而閘門不會等人,關掉選單的時候那一排已經結算完了。
  擋的地方是 `hooks/useNoContextMenu.ts`,**掛在 app 層不掛進 LaneRunner**(理由跟音樂一樣:
  那個元件每一關重新掛載,交棒的 900ms 之間會沒有防護)。順帶要關掉 `user-select`,
  不然拖過去會把畫面上的文字反白,放開手一樣跳出瀏覽器自己的「複製 / 全選」。
- **不要在 PanResponder 裡加 `nativeEvent.button !== 0` 的保險。** react-native-web 在
  responder 系統那一層就先擋掉非主鍵了(`isPrimaryPointerDown`,連 macOS 的 ctrl+click
  一起擋),右鍵根本走不到 `onStartShouldSetPanResponder`;而 RNW 組給 responder 的
  nativeEvent 是自己拼的物件,**裡面沒有 button 這個欄位**(`createResponderEvent.js`),
  讀出來永遠 undefined、條件永遠成立。看起來更嚴謹,實際一行都沒生效——
  跟 `box.bottom`、RNW 的 `filter` 是同一類的坑。

**設定與音訊**
- **手寫滑桿要用「畫面座標」算比例,不能用 onLayout 的 `layout.x`。** 那個 x 是相對於
  **父層**的,而滑桿的父層是面板內容區、面板又置中在整個畫面上,兩者差幾十像素;
  拿去減 `gesture.moveX` 會讓整條偏掉(實測拖到 75% 的位置,值直接飽和成 100%)。
  正解是在 `onPanResponderGrant` 用 `gesture.x0 - e.nativeEvent.locationX` 反推軌道左緣
  (不必動用 `measureInWindow`,那是非同步的,回來時手指早就移走了)。
  而且 Grant 用 `locationX`、Move 用 `moveX - 左緣`:**兩個事件不能用同一個欄位**,
  移動事件的 `locationX` 是相對於「當下命中的那個元素」,拖出軌道之後就跳掉了。
- **音量要獨立一個 effect,不能跟「播/停」擠在一起。** 擠在一起的話拖滑桿的每一格
  都會重跑一次 `player.play()`,而網頁上那是一次 seek + 重新解碼,拖動時聲音一直斷。
- **音效播放器不能綁在元件上。** 觸發點散在各處(按鈕、通關、選技能),而 LaneRunner
  每一關重新掛載——綁在裡面每一關會重建一次(跟背景音樂那個坑同一個形狀)。
  現在是模組層單例 `playSfx(id)`,而且**延遲到第一次要播才建 player**:
  開場就建七個等於七個檔案一起解碼,主介面會卡一下。
- **暫停只能有一份。** 要停的東西有兩處(距離的計時迴圈與波次結算),各自實作一定會漏掉
  其中一個,而漏掉的症狀是「畫面停住但怪還在往前推進」——關掉面板的瞬間人數突然少一截,
  完全看不出原因。所以設定面板與抽地圖面板走的是「挑技能時暫停」**同一個 `paused`**。
- **重抽一定要保證換一張。** 10 選 1 有十分之一的機會抽回同一張,而玩家按下「重抽」
  看到一模一樣的名字,第一個念頭是「按鈕壞了」——那是隨機性最不划算的一次現身。
- **生存模式的地圖要抽在 app 層,不能抽在 LaneRunner 裡。** 生存模式一關接一關,
  而 LaneRunner 每一關換 key 重新掛載——抽在裡面的話每過一關地圖就換一次,
  而抽籤要給的是「這一輪的身分」不是「這一關的裝飾」。

**素材轉檔**
- **`img.convert('RGBA')` 會把 GIF 的 `transparency` 索引一起套用。** GIF frame 的 `info` 帶著
  `transparency: 76`,`convert('RGB')` 會把那個欄位**複製過去**,接著 `convert('RGBA')` 就拿它
  去把純黑打成透明——史萊姆的眼睛(純黑)整片被挖空,而且跟去背邏輯完全無關,
  查起來會一直以為是連通填充的錯(我在這上面繞了很久)。
  要顯式組 alpha:`Image.merge('RGBA', (*img.split()[:3], Image.new('L', size, 255)))`。
- 去背用「從邊界連通填充近黑」,不能整片去黑——眼睛就是黑的。**閾值要壓在 10**,
  16 會沿著身體邊緣的暗像素漏進眼睛。改閾值一定要重驗眼睛區域的透明像素數是 0。
- 兩格動畫要對齊**身體重心**,不能用整張圖的 bbox:噴刺與飄浮的小點不對稱,會把中心帶偏
  (國王實測兩格差 103px,直接播會左右跳)。敵人那 60 組是 `scripts/align-frames.py`
  在建置時對齊的(重心 + 底邊,同一張畫布),畫面端只換 source。
- **對齊用的畫布要長大,不能沿用待機格的。** 裁到待機格的大小,巨蟒會掉 20.5% 的不透明像素
  (牠的動作格是立起來的,比待機高一截)、魔王四型掉 16.2%——那是把頭切掉。
  畫布長大之後角色會被 `contain` 縮小,所以 align-frames 同時輸出 `wRatio/hRatio/anchor`,
  畫面照那組比例畫框(底邊踩地面線、重心對中心),大小才跟長大之前一致。
- **CSS `filter` 在 react-native-web 上會被丟掉,不要拿它做元素換色。** RN 0.86 的型別有
  `filter`,型別檢查也過,但實測 computed style 是 `none`——RNW 只編譯它認得的樣式屬性。
  症狀是「程式看起來完全正確,畫面卻一點變化都沒有」。元素換色維持「原圖 + 半透明
  tintColor 疊層」,真・換色只能走 build-time 產圖(288 張,見 docs/DESIGN.md §8.3)。
- **投擲動作要跟真的飛出去的那一把對上。** 照固定週期播的話會變成「他在做動作,
  但武器是另一個時間點飛出來的」。所以 hook 記 `enemyThrowAt`(誰、什麼時候出手),
  畫面只在那之後的 260ms 換成投擲格——而且要短於出手間隔(620ms),
  不然輪到的那個人會一直卡在投擲的姿勢,看起來像定格。
- **滿版底圖上的暗紗要逐張算,不能全部套同一個透明度。** 這 10 張大關底圖本身的亮度差
  三倍以上(草原 0.15、荒漠 0.52)。套同一個透明度的話,荒漠那幾個大關的地面比草原亮三倍,
  而閘門、傷害數字、提示文字的顏色是固定的——**同一組前景在不同大關的可讀性完全不一樣**
  (實測荒漠關的「撞上石頭」幾乎讀不出來);反過來把紗加重到荒漠剛好,草原就整片黑掉。
  現在是 `shrink-backdrops.py` 照每張圖的平均亮度反推透明度(目標亮度 0.13),
  跟底色、分隔線色一起寫進產生檔,畫面層只管照著疊。
- **對齊後的動畫格要縮圖再打包。** 來源是 400~750px,而畫面上小怪只畫 42px、魔王 132px;
  直接打包是 39MB(為了 42px 的東西載 300KB)。照用途各自設上限(留 3x DPR 餘裕)之後是 5.7MB。
  **只縮動畫格,原圖不動**——轉職畫面的立繪還是要大張的。

**自動化測試(這些坑讓我以為測過了,其實根本沒測到)**
- Playwright 的 `boundingBox()` **只有 x/y/width/height,沒有 bottom/right**。
  拿 `box.bottom` 去比較永遠是 `undefined`,條件靜靜地全部失敗——機器人看起來在跑,
  其實一步都沒操作。**寫完操作腳本先斷言「操作真的有生效」(例如角色 x 有變),再看結果。**
- 手寫 `new TouchEvent(...)` 派送到 `elementFromPoint` 不會被 RN 的手勢層收到,
  會誤判成「觸控壞掉」。**觸控一律用 CDP `Input.dispatchTouchEvent`。**
- `text=鍛鍊` 這種文字選取器會選到畫面上其他同字的文字(例如底部「已帶:鍛鍊 1」),
  點下去沒反應。**要點的元素一律給 `accessibilityLabel`,用 `[aria-label^="..."]` 選。**

**危險操作**
- **「重新再來 / 放棄遊戲」要按兩次,而且不另外開確認視窗。** 面板本來就蓋在跑道上了,
  再疊一層會變成「面板上的面板」,小螢幕塞不下。第一次按把字換成「再按一次確認」
  並轉成危險紅,第二次才執行。兩顆排在「繼續」下面——玩家打開這個面板九成是要暫停
  或調音量,把丟掉整場的按鈕放最後才不會手滑。
- **重來只要換 `runKey`,不要在原地 reset。** 每一場都是一個新的 LaneRunner 實例,
  跑到一半的狀態全部跟著實例走;在原地一項一項 reset 很容易漏掉其中一項,
  症狀是「上一場的怪出現在這一場」。
- **重來不結算這一場的金幣與掉落。** 結算了才重來的話,反覆重開第一波就能刷圖鑑掉落。
- **生存模式的「重來」是整輪重來,不是這一關重來。** 只重開這一關等於把「不能重試」
  那條規則從後門打開(卡在第 8 關就一直重開第 8 關),而那條規則是生存模式的全部壓力來源。
- **「放棄」走跟陣亡完全一樣的路徑**,不另外做一條:生存模式照樣結算分數——
  撐了十幾波才按放棄卻把波數丟掉,等於懲罰「主動結束」,而那跟陣亡是同一件事的兩種到達方式。

**技能組**
- **通關獎勵(技能書、圖鑑碎片)可以放心調大,因為它們都在理想路線之外。** 兩樣都只放大
  元素與主動,而那兩類對理想玩家的價值是 0(他每一波全清),敵人一格都不會跟著長。
  所以那幾個數字調的是「多久練滿一輪」這條**節奏**,不是難度——改它們不必重掃
  `ENEMY_POWER_RATIO`。**加倍長的小關(x-5 / x-10)一律雙倍**:它花的時間是兩倍,
  獎勵不跟著走的話玩家會學到「跳過長關比較划算」,而那兩關正是段落的中點與魔王關。
- **永久技能整組移除了,而它記著一條仍然成立的規則。** 鋒鑄/強化/堅韌/工藝 依指示拿掉
  (連同「每關通關三選一」那個畫面)。留意:**它是唯一一條會進理想路線的養成**,
  所以幅度上限必須跟「一次失誤的容錯緩衝」一起看(當時是 +45%)。哪天要加回類似的東西,
  那條約束要一起加回來——現在剩下的三條裡只有轉職進理想路線,由 verify-lane-jobs 在盯。
- **移除養成線的時候,舊存檔的欄位要丟掉,不要折現成別的東西。** 折現等於偷偷把一條
  已經廢掉的線換成另一條,而玩家沒同意過那筆交易;而且永久技能進理想路線、
  技能書不進,兩邊的性質根本對不上。留著不讀也不行——沒人讀的欄位是
  「以後會有人以為它有用」的陷阱。
- **技能書敢開到 100 級,是因為它在理想路線之外,不是因為幅度算得剛好。** 它只放大
  元素與主動,而那兩類對理想玩家的價值是 0(他每一波全清)。敵人不會跟著長,
  所以它是純粹的「抬地板」。對照:永久技能進理想路線,所以只敢給 +45%,
  而且 `verify-lane-skills` 有一項在盯「養成總倍率 vs 容錯緩衝」。
- **100 級的曲線要前段陡、末段仍在爬。** 線性的話前 20 級每級 +1.5%,而那是絕大多數
  玩家所在的區間——通關的主要獎勵拿到卻無感等於沒給。指數飽和則相反:末段幾乎平的,
  滿級前 20 級跟滿級差不到 1%,「練滿」就沒有意義。冪次(0.7)兩邊都避開。
- **上限改了就要換算舊存檔,不能沿用數字。** 舊制滿書 x1.75 直接當成新制 5 級會變成
  x1.18——一次玩家完全不知道原因的削弱。`rescaleLegacyBookLevel` 換的是**倍率相同**的等級
  (5 級 → 37 級)。這款在 `bestSurvival` 從「關」改成「波」時踩過同一個坑。
- **改了取得速度就要一起改上限,反之亦然。** 上限開到 100 而掉落還是 2%~12% 的話,
  練滿要打上千場,那條線等於不存在。所以同一次把「通關保證給一本」一起做掉。
- **效果算得出來 ≠ 效果有生效。** 金・擴散的 `metalSpread` / `metalSpreadDamage` 在
  `runSkillEffects` 一直算得好好的,但 `useLaneRun` 的 `applyOnHit` **從來沒讀過它們**——
  所以那一款在遊戲裡既沒有擴散傷害也沒有任何畫面,只有結算時的 `pierceRatio` 在動。
  症狀是「這款好像沒作用」,而因為結算數字仍然會變,查起來完全不像漏了一段。
  **新增任何 `RunSkillEffects` 欄位,都要順手確認 hook 那邊真的有人讀它。**
- **回饋文字要自己收掉,不能等下一則來蓋。** 「沒碰到」「數量 -1」那幾行是**事件**不是狀態,
  留在畫面上會被讀成「我現在的狀況是沒碰到」,而且下一排逼近時它還在跟閘門搶注意力。
  而且**光在畫面層算「過了幾毫秒」不夠**:那個判斷只在重畫時才跑,跑圖一停
  (陣亡、通關、打開設定面板)tick 就停了,最後那一則會凍在結果卡底下。
  要在 hook 裡掛 `setTimeout` 真的把它清掉(`FEEDBACK_MS` = 2 秒)。
- **技能組不准有「最高數量上限」。** 清怪效果只有兩種合法寫法:
  **固定隻數**(`WaveBoost.kills`,越落後越有用)或**吃自己戰果的比例**
  (`WaveBoost.chainRatio`,越強越有用)。「整波的幾成」那一路已經整條拆掉——
  它是一個**跟隊伍無關的數量天花板**:對弱的人是零頭,對強的人是上緣,兩邊的好處都拿不到,
  唯一的效果是讓數字看起來有在動。金・擴散原本就是那樣(等級 2 剛好是「整波的 12%」),
  現在跟雷同軸。`verify-lane-run` 有一項直接證明「整波 5 隻與 500 隻算出來一樣」。
  注意這跟 `slow` / `freezeChance` 那種**機率上限**不是同一件事:那兩個夾的是
  「發生的機率」不是「清掉幾隻」,而機率本來就只能到 1。
- **格數一多就要有「專精獎勵」,不然廣度變成無腦最佳解。** 18 款配 10 格,如果「拿新的」
  永遠不虧,每一次選擇就只是「挑一個還沒有的」——那不是決策是排序。集齊同元素三階
  給傷害加成之後,深度才是一條真的路線。(格數曾經因為只有 6 款而降到 4,
  那是「格數必須少於款數」;款數變 18 之後 10 格照樣成立。)
- **集齊加成一定要乘在傷害上,不能給戰力。** 給戰力就會被貪心看到,敵人立刻跟著長——
  鋒刃/增殖 就是為此被拿掉的。乘在傷害上則對理想玩家等於 0(他每一波全清),
  所以照樣不進理想路線,`maxRunSkillAttackMultiplier()` 仍然是 1。
- **「二三階造成傷害」仍然不進理想路線,而且理由跟前四次一模一樣。** 敵人戰力照
  「這一場的最佳路線」算,而最佳路線假設**每一波全清**——多給理想玩家清怪的能力等於 0
  (`extraKills` 還會被夾在整波隻數上)。真正吃到的是漏了幾隻的你:越落後越有用。
  實測 18 款上線前後,第 12 關的準確率曲線是 100/81/62/39/23 → 100/81/61/39/23,
  單場放大量 110 倍完全沒動。**傷害因此一律給固定隻數,不給百分比**:
  百分比會隨理想戰力一起長,等於偷偷變成一條跟敵人曲線平行的線。
- **轉職的主動額度不能用 `ACTIVE_SKILL_IDS.slice(0, N)`。** 只有四款主動時那樣還行;
  改成六元素各兩款之後,切前 N 個等於**只解鎖火跟金**——「沒有萬用元素也沒有廢元素」
  當場失效,而那是六元素設計的地基。現在轉職給的是「同時帶得動幾款主動」,對六元素中性。
- **二三階的 id 要能查回它的一階(`elementOf`)。** 相剋、圖鑑加成、屬性染色全部走那一支:
  `fire3` 對上被火剋的屬性一樣要放大,圖鑑收滿火的那幾件也要一起放大火的三階。
  照 id 直接查圖鑑的話二三階永遠查不到(表裡沒有那個 key),等於**安靜地少了一半加成**。
- **一階的 id 刻意不加後綴**(`fire` 而不是 `fire1`):圖鑑的 501 個條目、波次屬性、
  相剋表全部拿那六個字串當 key,加後綴等於要同時改存檔與圖鑑對應表。
- **同一族的三階共用一階的字形,只加階級小點。** 各畫一張互不相干的圖反而讓玩家
  看不出它們是一家的,而「這是我投資過的那個元素」正是技能列上最該一眼認出的事
  (四格裡常常有兩格同族)。點畫在左上,因為右下給了等級、正中央是字形。

**判定與畫面的一致性**
- **怪走到你身上就要當場扣人,不能等整段戰鬥跑完才一次結算。** 一波的怪鋪在結算點
  **前面** 624 個距離單位,所以第一隻碰到你之後還要 5.6~13.9 秒才結算(第 1 關 13.9 秒、
  第 100 關 5.6 秒)——那段時間裡沒打死的怪走到你身上就消失、什麼都不扣,
  然後整筆損失在結算點一次到位。跟「敵人的武器只存在畫面層」是同一種 bug,解法也一樣。
- **但那是「預付」,不是另一套規則。** 總額仍然由結算的 `leakLoss` 說了算,
  跑圖途中扣掉的當 `WaveBoost.leakAdvance` 交出去,結算只補差額(預付多了就退回去)。
  模擬器不設這個欄位,所以整條難度曲線一格都沒動——跟 `hazardResolved` 同一個手法。
  `verify-lane-run` 有一項證明「預付 0 / 一半 / 全額 / 超收,總扣人數完全一樣」。
- **判斷哪一隻算漏接,要用「注定會倒」而不是「已經倒了」。** 怪是一路衝過來的、刀是一把一把
  丟的,排在最前面那幾隻常常在還沒挨滿刀之前就走到你身上了。用「已經倒了」判的話
  它們會被算成漏接,但結算的公式(`waveKillCount`)算的是**整段戰鬥打得掉幾隻**,
  那幾隻在公式裡是死的——兩邊不一致就等於難度被悄悄改掉。
  射程外的那幾隻也不能算:它們「沒掃到」,不扣人也不給吸收(見 `resolveEnemy` 的 `missed`)。
- **判定寬度就是隊伍寬度,所以隊形的橫向座標只能有一份。** 放在 `game/laneRun.ts` 的
  `SQUAD_DX`(offset 單位),畫面乘上跑道寬度去畫;`dy`/`scale` 是純視覺,留在 components。
  兩邊各留一份的話,改了其中一份就會出現**「看起來碰到了但沒反應」**——這款最不能有的一種 bug。
  單位用 offset 不用像素也是必要的:像素會隨螢幕寬變,判定框就跟著螢幕變
  (實測 640/667/780/844 四種高度,隊伍的相對半寬都是 0.091)。
- **一個人的身體半寬有上限 0.075,那是「起跑什麼都吃不到」撐得住的極限。**
  第 1 關閘門左緣在 0.58,起跑位置 0.5——半寬只要 ≥ 0.08,站著不動就會吃到右邊那格,
  而「不動的話什麼都吃不到」是這款的第一課。verify 有一項專門盯這個。
  附帶結果是判定框比畫出來的史萊姆窄約兩成,方向是刻意的:**寧可窄一點**,
  寬過頭會讓玩家吃到他覺得沒碰到的格子(包含陷阱),那比漏接更難接受。
- **身體判定讓「閘門變窄」(第 41~400 關那一段)幾乎失效,這是已知的代價不是 bug。**
  隊伍長到八九隻時半寬就有 0.239,比最窄的閘門(半寬 0.12)還寬——一團十四隻的史萊姆
  本來就蓋滿自己那一格。實測手抖 ±0.25 的過關率從 62% 變成 100%,要拉到 ±0.45 才咬得動。
  要讓「手要準」重新變成後段的難度來源,得換一顆旋鈕(例如讓閘門的**位置**在跑道上
  左右浮動,而不是只把它變窄),那是一次獨立的設計決定。
- **不要在跑道上畫規則的輔助線。** 當前跑道的反白、兩格之間的分隔線與虛線、判定線,
  三個都拿掉了:它們是在替規則畫註解,而規則本身已經改成看得懂的東西
  (身體碰到框就算)。畫著反而在跟它搶注意力,還會讓人以為判定是「中心點越線」。
  它們兼著做的「地面在動」現在由底圖捲動負責。

**數值與單位**
- 裁切門檻拿像素常數去比距離值(兩者差約 10 倍),物件會在勇者身上多賴將近一秒。
  距離與像素是兩個座標系,常數命名要看得出是哪一個。
- 雜湊常數只要含有 `LANE_COUNT` 的因數,取餘數之後每一隻都會算出同一條跑道。
  用 `Math.imul` + 位移混合,而且要有「長期分佈平均」的驗證。
- **兩條各走各的指數曲線,遲早會岔開。** 敵人曾經自己走 `1.9^tier`,跟閘門的成長完全沒關係;
  兩個數字單看都合理,湊在一起是最佳玩家的領先幅度每過一排敵人再乘 3 倍——第 10 關實測
  3.5x → 9.5x → 17.6x,等於前三個閘門決定勝負,後面整場都在跑完流程(使用者回報的
  「中段沒有挑戰性」)。現在敵人戰力是**逐場模擬「這一場的最佳路線」**算出來的(見 `createRun`),
  所以最佳玩家在每一排都恰好領先 `1/ENEMY_POWER_RATIO` 倍,「選對就一定過」是結構保證。
- **用「平均理想路線」估敵人也不夠。** 中間版本是拿好閘門的幾何平均去推,固定 +N 之後成長
  不再是等比,各場抽牌差異被放大:抽到一堆裝備格的那幾場,最佳玩家的領先幅度會從 8.6x
  掉到 1.8x,同一顆 seed 甚至讓「每排都挑最好」也過不了關。平均值不能代表個別場次。
- **任何養成都不准給「起跑人數」。** 起跑總戰力是固定的,人數只決定怎麼拆
  (`perHero = 總戰力 / 人數`);而跑道上的「勇者 +N」是固定值,收益 = `N x perHero`,
  所以起跑 6 人的玩家每一格只拿到 1/6。技能「增援」原本每級 +1 人,實測第 25 關 85% 準確率
  是**全開 22% vs 裸裝 54%**——點滿技能比什麼都不點還慘。已改成加血,verify 有一項專門盯著。
- **單場的戰力膨脹會吃掉整條養成曲線。** 閘門幅度原本是裝備 x1.6 / 勇者 +60%,一場 15 個閘門
  把戰力放大 **2100~6900 倍**(第 1 關 10 → 24528)。三個問題:數字大到分不出 13160 跟 67368、
  前半段的選擇在數值上被抹平、而且**單場雪球比整條關卡進度(25 關 5.8 倍)大 460 倍**——
  裝備/經驗/技能書做得再多都感覺不到。現在壓到 98~148 倍(長關 339~922),數字停在 4 位數。
  注意:**壓小閘門不會改變難度**(敵人照最佳路線走,會自動跟著小),但會改變「失誤的相對代價」,
  所以每次動 `GEAR_STEP` / `HERO_ADD_RATIO` 都要重掃 `ENEMY_POWER_RATIO`。
  **場內技能上線的時候就是這樣做的**:技能自己就有 2.4~4.3 倍,直接疊上去會把放大量推到
  136~357 倍(長關 2326),所以閘門讓出一點(`GEAR_STEP` 1.22→1.14、`HERO_ADD_RATIO` 0.22→0.14),
  再把 `ENEMY_POWER_RATIO` 0.50→0.53、`EASY_RATIO` 0.18→0.24 掃回原本的準確率曲線。
  讓出的是閘門不是技能:閘門一場觸發 15~20 次,少一點沒人察覺;技能一場只挑 5 次,
  每一次都該是「有感的決定」。
- **加倍長的小關不能只是「排數加倍」。** 10 波照 4 排一波 = 40 排 = 30 個閘門,而閘門數量是
  指數的指數:實測放大倍率從 54~134 倍衝到 **1833~4230 倍**(終點戰力 16 萬),把壓膨脹的工作
  整個抵銷;難度也一起爆掉——90% 準確率的過關率從 80% 掉到 44%,而那**不是設計決定的**,
  純粹是「路變長所以失誤機會變多」。現在長關是每 3 排一波(30 排、20 個閘門):
  波數翻倍、閘門只多三分之一,長關的「硬」來自敵人一波接一波,不是來自數字又翻幾十倍。
  另外 `LONG_LEVEL_RATIO_SCALE` 再把它變長的代價補回一部分。
- **收緊難度會放大養成的價值,兩者要一起看。** `ENEMY_POWER_RATIO` 從 0.37 收到 0.53 之後,
  容錯緩衝從 2.7x 變成 1.89x,而滿養成的起跑倍率是 2.17x——**養成的倍率超過了緩衝**,
  等於滿養成多送一次失誤額度,實測全開對裸裝的領先從 24pp 變成 40pp(養成本身完全沒動)。
  `verify-lane-skills.ts` 有一項專門盯「養成總倍率 vs 容錯緩衝」的比值。
- **爆發格改成每場固定次數,不要每格獨立抽。** 每格 12% 機率的話,一場會抽到 0~4 個 x2,
  而每個都是翻倍——單場放大量差 2.4 倍,玩家感覺到的是「這場運氣好」而不是「我選得好」。
  現在是每場固定 2 個(`DOUBLE_GATES_PER_RUN`),份量不變、隨機性消失。第一格不放爆發格:
  起手 1 隻,x2 只變成 2 隻,「翻倍」的畫面完全看不出來。
- **跑速在第 40 小關封頂之後,難度不能再靠速度。** 0.9 秒一排是「看清楚兩個選項 + 決定 + 滑」
  的下限,再快就是雜訊不是難度。後面 260 個大關只剩兩個方向:**要求更精準**(閘門變窄)與
  **讓失誤更貴**(陷阱變重、勇者波變密)。三顆旋鈕都碰不到完美玩家,所以結構保證不受影響。
- **一段只轉一顆旋鈕,而且段與段之間要留空白。** 兩條斜坡重疊的那幾關會變成唯一一段
  「難度跳兩級」的地方,而那不是設計決定的,是排程沒對齊。現在是 41~400 閘門、
  501~1200 陷阱、1401 之後勇者波,中間刻意空著;`verify-lane-run` 有一項在掃
  「任何相鄰的兩關之間最多只有一顆在動」。
- **難度指標不要用「亂選過關率」。** 跑道 20 排(15 個閘門)之後,亂選在任何難度設定下都是
  0~1%——每格好壞差 2.6 倍,亂選等於 2.6^-7.5,不是調參救得回來的。硬要把它拉回 20% 只會
  逼著把難度調到沒有意義的低點。改用「準確率 → 過關率」曲線(見 `verify-lane-run.ts`),
  現行值(一般小關):100% → 100%、95% → 72%、90% → 50%、85% → 30%、80% → 18%。
  加倍長的小關比較硬(90% → 45%),第一大關則刻意寬鬆(容錯係數 0.18 一路拉到 0.47)。
- **「一波多少隻」是打擊手感的旋鈕,不是難度的旋鈕。** 隻數翻倍(`ENEMY_UNITS_PER_HERO` 1→2)
  實測只把 90% 準確率的過關率從 61% 推到 57%——因為 `absorbedFrom(units)` 也跟著翻倍,
  理想人數長 55%,敵人曲線自動追上去。**要調難度只能轉 `ENEMY_POWER_RATIO`。**
  翻倍真正買到的是密度:第 12 關視野內同時 9 → 18 隻、第 102 關 4.7 → 9.5 隻,
  而火的燃燒擴散與雷的連鎖閃電都是「找旁邊還站著的那幾隻」,四五隻的時候經常找不到目標。
  兩個連帶動作:**`MAX_WAVE_SIZE` 一定要跟著開**(留在 24 的話十波裡六波封頂,等於白改),
  以及 **`ABSORB_RATIO` 要跟著降**(吸收翻倍會把長關的單場放大量從 662 推到 777 倍,越線)。
- **「一波幾隻」的天花板要長在吸收上,不是長在隻數上。** `MAX_WAVE_SIZE` 原本是 48,
  但它真正在擋的是**吸收 → 理想人數 → 下一波幾隻**那條複利迴圈;直接拿掉會讓長關的
  單場放大量從 662 衝到 1666 倍。把上限搬到 `ABSORB_UNIT_CAP`(吸收最多照 48 份算)之後,
  「一波要畫幾隻」跟「吸收算幾份」就分開了:隻數可以一直長,理想路線一格都沒動。
  **夾的是「隻數 x 份量」的乘積**——只夾隻數的話精英波(一隻抵 6 份)會從天花板底下溜過去
  (實測 662 → 693)。而且隻數一放開,**每一隻變弱、漏掉的隻數等比變多**,
  90% 準確率的過關率會從 50% 掉到 42%,所以 `ENEMY_POWER_RATIO` 要跟著掃回來(0.51 → 0.48)。
- **隻數沒有上限之後,「畫幾隻」要另外設上限。** 第 1 關最後一波是 152 隻擠在 549 的距離裡,
  換算到畫面上每隻只相隔 5.6px 而圖是 42px 寬——**87% 互相重疊**,玩家看到的是一堵牆,
  瀏覽器卻老實地畫 300 多張圖(實測掉到 9 fps)。`MAX_DRAWN_MONSTERS = 64` 只畫最近的那幾隻,
  視覺上完全一樣(多的那些本來就被蓋住),fps 回到 30 以上。
  同一個道理也適用在染色:一隻怪同時只疊**一層** tintColor(凍結 > 燃燒 > 遲滯 > 屬性),
  四層各疊一張的話 89 隻就是 356 張圖,而且顏色混在一起玩家反而分不出狀態。
- **`fireIntervalMs` 的 90ms 下限不是真的下限。** 它回傳之後還要除以 `volleyRate`(最多 x4),
  所以只拿那個常數去算「這一波丟不丟得完」會誤判成打不完——實測終場 96 人是 23ms 一發,
  一波丟得出 576 下而只需要 144 下。
- **驗收「中段有沒有挑戰性」要看死亡分佈,不是看過關率。** 過關率一樣是 60%,可能是
  「全死在第一排」也可能是「一路撐到最後」,兩者體驗天差地遠。現行值(85% 準確率):
  10 波各佔 3 / 14 / 11 / 19 / 13 / 8 / 14 / 4 / 5 / 9 %——第一波只佔 3%,後半段仍佔 40%。
- **跑圖模擬只能有一份(`scripts/simRun.ts`),verify 腳本不准自己寫迴圈。** 三份 verify
  原本各自寫了一圈「產生跑圖 → 每排選一格 → 結算」,場內技能上線之後只有一份被改到,
  另外兩份的玩家少了整條技能曲線,而敵人是照「含技能的最佳路線」算的——結果是
  `verify-lane-jobs` 報「未轉職 + 每排都挑最好 = 0% 過關」、`verify-lane-run` 報
  「拉得再準也 0% 過關」。兩個都不是真的壞掉,是模擬器沒跟上,而且看起來完全像是設計壞了。
- **場內技能的選項要綁 seed,不能即時亂數抽。** 敵人戰力是逐場模擬「這一場的最佳路線」算的,
  而最佳路線包含技能;四選三會漏掉一個,漏掉的剛好是這次最該點的時候,玩家就走不到理想曲線上。
  用即時亂數的話 `createRun` 根本不知道漏了哪個,只能拿「假設永遠開得出最佳選項」的平均表去估,
  實測領先幅度會在 1.41x~2.70x 之間漂(目標是每一排都精確 1.89x)。綁 seed 之後
  `createRun` 用 `runSkillOffersAt` 重播同一串選項,結構保證才回得來(實測 2.41~2.46x)。
  這跟閘門那邊「平均理想路線估不準」是同一個錯誤的第二次現身。
- **屬性不要綁死在怪物原型上。** 12 種原型配 8 種屬性除不盡:4 個屬性會多一倍的出現機率,
  剋它的那幾款技能就永遠比較好,「沒有萬用元素也沒有廢元素」當場失效。改成**顏色**承載屬性
  (一波共用一個屬性、整群疊一層 `tintColor`),屬性維持精確平均,而「一波混三種造型」也留得住。
- **相剋要逐元素算,不要用一個純量放大整組。** 純量版(`WaveBoost.counter`)會把主動技能一起放大,
  帶對屬性就是整體 x2.5——另一種買到勝利;而且玩家看不出自己是靠哪一個元素贏的。
  現在放大(x2.5)與削弱(x2/3)都在 `runSkillEffects(skills, waveElement)` 逐元素結算完,
  `WaveBoost` **連 counter 欄位都不留**——留著就會有人再用一次。
- **勇者波的屬性要走另一條 salt。** 關卡前會公開整關的屬性順序(押注才成立),
  但十波全公開的話「通用技能」就沒有位置了。勇者波(每 3 波一次)另抽一條、提示列標「?」,
  押注押得再準,每三波還是有一波要靠通用扛。
- **勇者波的敵人不能照玩家的職業畫。** 接成 `job?.archetype ?? null` 的話,第 1 大關(還沒轉職)
  會退回學生那張圖——**整波敵人長得跟玩家自己一模一樣**(玩家的勇者就是學生那隻史萊姆)。
  而且照玩家職業畫還有第二個問題:轉職越高敵人立繪越華麗,看起來像養成把敵人養大了。
  現在走 `enemyHeroLookForRow(stage, rowIndex)`:造型照排號輪替,階級跟著關卡走。
  (放在 `laneJobs` 不放 `laneRun`——laneJobs 已經 import laneRun,反向就是循環相依。)
- **不要在地上畫「危險帶」。** 勇者波的落點原本是獨立雜湊,畫面只能畫一條紅色帶子——
  而那個位置沒有任何人站著,看起來像地形不像攻擊。落點改成**投擲者站的那條線**
  (`hazardsFor` 直接取 `waveMonsters` 的 offset,邏輯與畫面同一個算式),
  就可以直接畫他丟出來的武器直線飛下來,帶子整個拿掉。
- **勇者波是「活著的人都在丟」,不是固定一個投擲者。** 固定一個的版本有兩個講不通的地方:
  一整波二十個勇者只有一個在動(其他人像背景),而且**被你打倒的人照樣在丟**。
  改成 `hazardsFor(rowIndex, units, survivors)` 之後,威脅剛好等於「你沒打完的部分」:
  全清就一發都沒有(所以完美玩家不受影響,它照樣不進理想路線),漏 k 個就有 k 條線。
  代價是躲的難度換了一個量級,所以 `HAZARD_LOSS_RATIO` 要從 0.25 降到 0.14——
  不降的話勇者波會變成「打不完就等於全滅」,而它每 3 波來一次,整條準確率曲線會被拉下去。
  **落點因此不能在產生排的時候就固定**(它取決於玩家當下的戰力),`EnemyEffect` 改成存 `rowIndex`。
- **敵人的武器要是「真的在飛的物件」,不能只存在畫面層。** 它原本是用 `Date.now()` 推出來的
  動畫,傷害則在「這一排結算」的那一瞬間才算——結果是玩家看著武器穿過身體卻什麼都沒發生
  (使用者回報的「被攻擊到沒有任何負面效果」)。搬進 hook 變成真的物件之後,飛到你身上
  那一刻就扣人。`WaveBoost.hazardResolved` 負責告訴 `resolveEnemy` 別再算第二次;
  **模擬器不設這個旗標**,照舊在結算時算——規則、幅度、一波最多一次都一樣,兩邊不會岔開。
- **同時投擲的人數要有上限,那個上限就是「還閃得掉」的保證。** 落點寬 0.26,兩個人最多蓋掉
  0.52,跑道中間永遠留得下一段空的;放開讓十幾個人同時丟就會蓋滿整條,閃避從「看得懂就躲得掉」
  變成「站哪都會被打」——那不只是難,是**理想玩家也躲不掉**,敵人曲線的結構保證跟著失效。
  「每個人都要丟」靠**輪流**(`activeThrowers`)而不是「同時」。
- **要先出現在畫面上才能丟。** 沒有這個條件的話武器會從畫面外憑空出現在半空中,
  玩家看到的是「東西自己冒出來」而不是「那個人丟過來的」。
- **扣人一定要有畫面上的訊號。** 扣人本身只在 HUD 的數字上動一格,而玩家的視線在跑道上;
  沒有那一下紅閃,實際扣了人也等於沒發生過。
- **同一條線掛幾把要跟「有幾條線」一起看。** 只有一個投擲者的時候是 3 把(要連成一條看得出來的線);
  變成十幾條之後 3 把是畫面上 35 把,閘門整個被蓋住,反而看不出哪裡安全。現在是 2 把、而且畫小一號。
- **技能書(第三層養成)一律不准碰「選項串」。** 三條都試過而且都會壞:
  ①「選項變多」讓貪心更容易抽到鋒刃/增殖,理想戰力跟著上升;
  ②「保證選項裡有幾個元素」等於**擠掉**鋒刃/增殖(一次只有三個位置),
  實測 60 次選擇裡有 11 次讓貪心走到不同的組合;
  ③ 連「提高元素的等級上限」都會壞——滿級的元素會繼續留在池子裡把它稀釋掉,
  同一顆 seed 抽出來的三個就不一樣了。共同的教訓是**只要動到選項串,玩家側的曲線
  就會偏離 `createRun` 假設的那一條,而且方向不固定**。現在技能書只放大元素與主動的
  效果幅度,verify 直接證明「帶滿書的戰力曲線跟沒有書時一模一樣」。
  (鋒刃/增殖 後來已移除,所以①那條理由不再有對象——但**三條教訓照舊成立**,
  哪天加回會加戰力的技能,選項串就又會變成一條通往理想路線的暗路。)
- **生存模式不要另外長一條敵人曲線。** 它用的就是既有的關卡曲線,差別只在**不能重試**:
  一般模式死掉重打同一關,所以失手幾乎沒有代價;生存模式連續闖關、死了就結束。
- **生存模式的「連續」是指不中斷,不是指數值不重置。** 通關不畫結果卡、不等玩家按鈕,
  過場 900ms 直接接下一關(見 LaneRunner 的 HANDOFF_MS);但**每一關仍然從 1 人重新滾**。
  想過「數值一路延續」但做不到:`IDEAL_PATH` 只有 48 格(`MAX_ROWS_PER_RUN + 8`),
  一關就用掉 20 格,延續到第三關就查表越界 → `idealStep` 夾在最後一格 →
  「勇者 +N」不再跟著人數長,閘門對已經滾出上百人的玩家變成零頭,敵人曲線也跟著凍住。
  而且延續下去戰力是每關 x264 的複利,五關就 10^12,數字整個看不懂。
  **「從一個人變成一支隊伍」本來就是一場跑圖的弧線**,連續的是體驗不是數字。
- **分數的單位要跟玩家心裡數的東西一致。** 生存模式的分數從「連過幾關」改成「撐過幾波」:
  關數每十波才動一格,玩家在跑圖途中完全看不到自己的分數在長。存檔 v3 → v4 有一條遷移
  把舊的關數 x10 換算成波數——不換算的話,撐過 21 關的老玩家會變成「撐過 21 波」,
  技能書等級當場從 5 級掉到 0 級。
  另外做一條曲線就會變成「兩條各走各的指數」——這個專案已經在那上面摔過好幾次。
- **主動技能一律給固定值,不給百分比。** 百分比會被敵人曲線完全吸收(敵人照最佳路線算,
  而最佳路線包含技能),畫面很炫但難度一點沒動;固定值對滾出 80 人的最佳玩家是零頭,
  對剩 12 個人的你是活下來——**越落後越有用**。也因此它們不進理想路線(貪心只看戰力,
  永遠不會挑它們),不會讓敵人為了一個沒人用得到的東西變強。兌換率、閃避落點同一個道理。
- **冷卻現在綁「秒」不綁「波」——這一條翻過案,原本剛好相反。** 舊規則(綁波)的前提是
  「一波 = 一排」,而一排的時間 = 排距 / 跑速,跑速從 45 爬到 111,所以綁秒等於
  「越後面的關卡技能越弱」。**關卡結構改成「戰鬥段是時間不是排數」之後那個前提沒了**:
  `battleSecondsPerWave` 是用目標秒數回推距離算的,實測第 1 關 13.6 秒、第 40 關之後
  一路到第 3000 關都是 17.1 秒(差 1.26 倍,而且第 40 關就封頂),整個波週期 18.5 → 18.1 秒。
  綁秒現在比綁波穩,而且多了一件綁波做不到的事:**玩家看得到倒數**——
  綁波時畫面上只寫得出「還要 2 波」,那是玩家換算不成時間的單位。
  `verify-lane-run` 有一項在盯那個前提(一波的秒數在 3000 關之間差不到 1.6 倍),
  哪天結構又改回「一波 = 一排」,它會先紅。
  例外是「擋下一整波損失」的兩款(壁障、土・護盾):它們的量級本來就是**波**,
  所以秒數要開到 70~120 秒(≈ 4~6 波),不然會變成每一波都免疫。
- **燃燒不能是「直接補一下傷害」,要是持續傷害。** 第一版的擴散是 `hitsOn[j] += 1`,
  等於白送一整下攻擊;配上雷就爆掉——連鎖電到的每一隻會再各自觸發一次擴散(那是刻意的交互),
  一次命中可能瞬間送出十幾下,打得倒的那一批**同一瞬間全部消失**(使用者說的「跟雷搭配會無解」)。
  改成每秒啃 `BURN_DPS_RATIO`(5%)的最大生命之後,光靠燒要 20 秒才燒得死一隻,
  而一整波只有 13~17 秒——它永遠不會自己收掉一隻,跟雷疊起來也只是「燒到的目標變多」。
  連帶:`hitsOn` 現在會有小數,**暴擊的序號要取整**,不然同一下的判定會隨著小數閃爍。
- **元素的「演出參數」與「擊殺數」必須是兩組數字。** 火的擴散隻數、雷的觸發頻率、
  冰的凍結機率只決定命中那一刻長什麼樣;**能多打倒幾隻仍然只由 `burnKills` /
  `chainRatio` / `pierceRatio` 決定**(`extraKills` 統一算)。混在一起的話
  「火燒得更漂亮」就等於「難度悄悄降了」,而那不是設計決定的。
- **「凍在原地」要讓那一隻跟著玩家一起前進,不是把牠釘住。** 怪的世界座標是固定的、
  往前跑的是玩家——真的釘住世界座標等於「玩家自己撞上去」,方向完全相反。
  而且**兩格動畫要一起停**(用 `frozenUntil - FREEZE_MS` 反推該停在哪一格):位置停了、
  手腳還在動的話看起來是「原地跑步」,而原地跑步在這個畫面上跟「這一隻壞掉了」長得一模一樣。
  土的減速就是同一個機制的部分版本(跟著玩家前進 N%),兩款疊起來 = 慢慢逼近、偶爾完全停住。
- **「事後取消死亡」演不出來,要改成「事前擋掉一次攻擊」。** 光原本是復活(歸零時保住幾人),
  玩家看到的是「我死了但沒死」——那一刻畫面上沒有任何東西可以演,而且它跟結算是同一瞬間。
  改成護盾之後,它擋的是**看得見的那把武器**:有位置、有時機、有訊號(光圈閃一下)。
  抬地板的性質完全一樣(全清就沒有武器飛過來,完美玩家用不到),但玩家知道自己被救了。
- **有冷卻的就只有四款主動,元素一款都不准有。** 土原本是「每 N 秒擋一整波」,
  技能列因此要為它破例畫倒數。改成減速(純被動)之後規則變得單純:
  **有倒數的就是主動,沒有的就是被動**,玩家掃一眼就分得出來。verify 有一項在盯。
- **跑圖途中的演出與這一排的結算只能有一份數字。** 小怪一隻一隻倒下用的擊殺數
  跟 `resolveEnemy` 算的必須是同一支函式(所以 `extraKills` 是匯出的)。各寫一份的話,
  畫面上倒了 9 隻、結算卻算 8 隻——而兩邊分開看都完全合理,是最難查的一種不一致。
- **驗證單場放大量要看中位數,不要看最大值。** 場與場之間本來就有 1.6~1.8 倍差異
  (抽到幾個爆發格、幾個裝備格都會變),取 3~5 顆 seed 斷言最大值等於在測分佈的尾巴:
  加一個新技能就會讓洗牌位移、檢查變紅,而設計其實什麼都沒退化。現在是 24 顆 seed 看中位數。
- **`tsconfig` 一度把 `scripts/` 排除在型別檢查外,而 `tsx` 用 esbuild 只剝型別不檢查。**
  結果 `simRun` 有一行讀早就刪掉的 `maxHp`,算出 NaN 塞進一個沒人讀的欄位,
  靜靜地錯了好幾個 commit,而 `npx tsc --noEmit` 全程是綠的。現在 scripts/ 已納入檢查。
- **關卡要拉長,就得把「波」跟「排」拆開,不能只是加排數。** 舊版一波 = 一排,一瞬間結算完,
  所以想把小關從 48 秒拉到 3 分鐘只能塞更多排 = 更多閘門,而閘門是指數的指數
  (實測 20 個閘門就把放大量從 148 推到 512 倍)。現在**戰鬥段是時間不是排數**:
  `rowDistances` 讓敵人排前面留一整段 `battleDistance`(怪一路衝過來的那段路),
  排數與閘門數完全沒動,關卡卻拉到 3 分鐘——而且拉長的那段是打架不是趕路,
  這正是使用者說的「好像是在選技能而不是在玩遊戲」的解法。
- **改完關卡結構,`GEAR_STEP` 那一組全部要重掃,而且範圍會整個位移。** 閘門從 15 個變 20 個之後,
  1.14 會把放大量推到 512 倍;掃到 1.07 才回到 220 倍。連帶 `ENEMY_POWER_RATIO` 0.53 → 0.42、
  `EASY_RATIO` 0.24 → 0.18 才把準確率曲線拉回原位(20 個閘門 + 10 波的失誤機會比舊版多很多)。
  **驗證腳本裡寫死的上下界也要跟著改**(`GEAR_STEP > 1.1` 那一項當場擋住正確答案)。
- **`LONG_LEVEL_RATIO_SCALE` 的存在理由消失了就要拿掉,不是留著調小。** 它本來是補償長關
  「閘門比較多所以失誤機會多」;改成波數翻倍、閘門不變之後,補償會讓長關**比一般關好過**
  (實測 74% vs 66%,方向整個反了)。現在是 1(不補償),長關的硬純粹來自 20 波。
- **血量拿掉之後,防禦軸只能是「兌換率」,不能是「多給人」。** 人數同時是戰力與生命,
  所以「加血」的自然翻譯是「加人」——但鐵則禁止養成給起跑人數(會稀釋 `perHero`,
  實測讓點滿技能比裸裝還慘)。改成 `tradeRate`(一個勇者換掉幾隻怪):它是乘數不是固定值,
  後期不會變零頭,而且**完全不失誤的玩家碰不到怪、拿不到好處**——抬地板不抬天花板,
  所以幅度可以放心給,也**不能算進 `createRun` 的理想路線**(算了就是讓敵人為了一個
  沒人用得到的東西變強,真人反而更難)。
- **一波幾隻要跟著理想人數走,不能是固定值。** 漏幾隻直接換算成少幾個人,
  兩群的規模必須同一個數量級:固定 5~9 隻的話,起跑 1 人漏 5 隻是瞬間全滅,
  後期滾出 50 人漏 9 隻是搔癢。現在是 `waveSize(idealHeroes)`,失誤的代價在每個階段一樣重。
  副作用已知:`MIN_WAVE_SIZE` 3 把最前面幾波架空了,死亡分佈從「往後遞增」變成攤平
  (第一排 0% → 18%),見 `docs/DESIGN.md` §10.5——解法在關卡結構那一步,不要單獨修。
- **只有「完美玩家也會付的代價」才准進理想路線,石頭是這條規則的第三次現身。**
  敵人戰力是照「這一場的最佳路線」算的,所以任何**只有失誤的人才會碰到**的東西都不能算進去——
  算了就是讓敵人為了一個沒人用到(或沒人付到)的東西變強,真人反而更難。
  已經出現三次:`tradeRate`(理想玩家不被撞,拿不到兌換率的好處)、
  場內技能的「壁壘」(同上)、以及**路障石頭**(理想玩家閃得掉每一顆)。
  實測石頭上線前後,「每排都挑最好」的過關率仍然是 100%、領先幅度仍然是 2.00~2.12x——
  完全沒動,因為 `createRun` 根本不知道有石頭這件事。**新增任何「只罰失誤」的機制,
  預設就是不進理想路線**,要進去之前先問「完美玩家會不會付這個代價」。
- **石頭要跟閘門搶反應時間的話,就等於偷偷把閘門變難。** 石頭一開始沒設淨空,
  結果是玩家在比較兩格閘門的那兩秒裡,前方同時冒出一顆要閃的石頭——難度上升了,
  但上升的是閘門的難度,而閘門的難度是 `ENEMY_POWER_RATIO` 校準過的。
  現在石頭離前一排閘門至少隔 `VISIBLE_AHEAD`(等閘門結算完才准進視野),
  離波次結算點至少隔 120(不然「撞上石頭 -N 人」跟「漏了 N 隻 -N 人」會同時跳出來,
  玩家分不清人數是被哪一件事扣掉的,兩筆懲罰疊在一起等於沒有回饋)。
  順帶一提**加速趕路要把石頭算成「前方有東西」**,而且用閘門的標準不是怪的標準:
  x4 速度衝向一顆剛冒出來的石頭,反應時間只剩四分之一,那是硬塞的失誤不是玩家的失誤。
- **挑技能的時候跑圖要停。** 試過不停(面板浮在跑道上、底下照樣跑),想解掉「一場切成
  打怪與選技能兩段」的問題。實際上變成同時要做三件事:讀三個選項、閃迎面而來的怪、
  還要選閘門——比原本更糟。翻回暫停。
- **人數是整數,技能的「x1.25」要跟理想路線用同一個取整函式。** 理想路線用浮點、玩家用
  `Math.round` 的話,2 人吃到 +25% 是 `round(2.5) = 3`(實際 +50%),領先幅度從 2.43x 漂到 2.80x。
  現在兩邊都走 `applyRunSkillPick`。順帶一提它保證**至少 +1 人**:1 隻的時候 `round(1 x 1.25) = 1`,
  玩家點了「勇者數量 +25%」卻什麼都沒發生,會直接認定這技能是壞的。

**教學、任務與副本**
- **教學關要「拿掉機制」不是「放水」。** 第一大關本來就有一條放水的斜坡(`EASY_RATIO`),
  但它解決不了真正的問題:第一場就有八種東西同時在動(閘門要選、石頭要閃、勇者波要躲、
  每打完一波還跳三選一)。新手學不會不是因為太難,是**同時要學的事情太多**。
  現在是逐關開放:1-1 只有閘門(而且兩格都是人數增減,好壞一眼看得出來)、1-2 加裝備閘門、
  1-3 加場內技能與精英、1-4 加石頭與勇者波、1-5 加爆發格與腰斬。
  **一關最多只新增兩種,而且成對出現的那兩種一定是同一課**(石頭與勇者波都是「閃開」,
  爆發格與腰斬都是「這一格是用乘的」)。verify 有一項在盯這個上限。
- **教學關的機制開關必須走 `createRun` 看得到的路徑。** 敵人戰力是逐場模擬「這一場的
  最佳路線」算出來的,所以拿掉爆發格/裝備閘門/場內技能之後敵人會**自己跟著變低**,
  結構保證(「每排都選對就一定過」)完全不必手動補償。反過來,如果有人在畫面層偷偷
  關掉場內技能而理想路線照算,玩家就會一路落後敵人——而症狀是「這關特別難」,
  不是「技能不見了」。這是 CLAUDE.md 的「兩側必須同時算」第三次出現,`runSkillPicksForStage`
  是唯一入口(createRun / useLaneRun / simRun 三邊都走它)。
- **教學關之間不能拿容錯係數互相比較,要比就比過關率曲線。** 1-5 是加倍長的小關
  (20 波、20 個閘門),失誤是複利的,所以同一個係數在它身上硬得多:照第一大關那條斜坡
  給它 0.313,實測 90% 準確率只剩 **29%** 過關——**跟大魔王關 1-10(21%)幾乎一樣**,
  而它是教學區的最後一關。現在教學關自己帶係數(`TutorialRules.enemyPowerRatio`),
  1-5 是 0.20,曲線剛好等於 1-6(「畢業考的難度 = 畢業之後的第一關」)。
  數字比 1-4(0.28)低**不代表比較簡單**,verify 的單調性因此改成比過關率——
  用係數比的話,這一項會在「1-5 被正確調鬆」的時候紅,那是完全反過來的訊號。
- **關卡變短要同時動波數與秒數。** `battleSecondsPerWave` 是用「目標秒數 ÷ 波數」回推的,
  所以只把 1-1 從 10 波改成 5 波的話,關卡總長完全沒變——變的是每一波的戰鬥段拉成兩倍長
  (怪從兩倍遠的地方衝過來)。那比原本更難熬。`targetSecondsForStage` 讓教學關按波數等比縮短,
  每一波仍然是約 18 秒,**短的是關卡不是節奏**,1-1 學到的手感在 1-6 照樣適用。
- **空欄位的文字要跟這一關實際的規則一致。** 技能列在沒有技能時寫「打完一波就能挑技能」,
  而 1-1、1-2 根本不給場內技能——玩家會一路等到終點,然後合理地認為這個功能壞了。
- **任務的「達成與否」一律現算,存檔只存「領過獎沒」。** 存一份達成狀態的代價是它會跟本尊
  不同步(玩家改過存檔、或哪天遷移動了 stage),而那種 bug 的症狀是**任務永遠完成不了**——
  玩家看得到卻做不到,比沒有任務更糟。只有「領過了沒」是算不出來的一次性事實,非存不可。
  連帶:v4→v5 **刻意不幫老玩家把已達成的任務標成已領**,不然升版的瞬間會默默沒收一批獎勵。
- **任務獎勵一律給金幣,不准給戰力或人數。** 那兩個是貪心(`bestRunSkillChoice`)看得到的,
  敵人會立刻跟著長;而任務是給新手的一次性獎勵,給戰力等於讓新手做完任務就買到一段難度。
  金幣則剛好接上副本的入場費——任務教你副本在哪,順便給你第一次的門票。
- **三種副本共用同一條敵人曲線,差別只能在規則與獎勵。** 想讓某個副本「比較難」,正確的做法是
  讓它跑**更後面的關卡**,不是另外調一組係數(「兩條各走各的指數」這個專案已經摔過好幾次)。
  現在的差別只有三樣:能不能重試、掉落量倍率、入場費。
- **單場副本通關不准推進 stage。** 副本打的是「目前進度的那一關」,讓它 +1 的話玩家就能靠副本
  推主線,而副本的獎勵是為「原地打轉」設計的,拿它推進度等於繞過主線的難度曲線。
  連帶:主介面那一行結果訊息**不能沿用 `lastResult`**,它是照 `stage - 1` 算標題的,
  副本沿用會少報一關(打贏 1-6 卻寫成「1-5 通關」)。
- **入場費要在進去之前扣,不是打完才扣。** 打完才扣的話,陣亡的玩家會在結果卡上同時看到
  「倒下了」跟「-336 金幣」,那看起來像懲罰(實際上他只是付了門票)。
- **「進過這個副本沒有」要在進去的那一刻記,不是通關才記。** 任務寫的是「進一次」,
  而玩家第一次進去很可能會死。

**每日循環與逐屬性養成**
- **逐屬性的資源,才撐得起「每天開一個屬性」。** 技能書原本是一個全域等級,而副本改成
  每天開一個屬性之後,那個屬性會退化成純粹的門票顏色——今天開火還是開雷,拿到的東西
  一模一樣,玩家沒有任何理由在意。改成六個元素各自 0~100 級之後,「今天是火」才是一則
  真的消息:它決定你今天能推進哪一條線。**新增任何「每日輪替」的東西之前,先問
  「輪到不同的那一天,玩家拿到的東西真的不一樣嗎」**——不一樣才有輪替的意義。
- **每日次數要存「哪一天 + 打了幾次」,不能存「還剩幾次」。** 存剩餘次數的話,跨日要有人
  負責把它加回去,而那個「有人」只能是某次讀存檔或某個計時器——玩家整天沒開遊戲、
  或開著遊戲跨過午夜,兩種都會漏掉。存日期則是**自我修復**的:今天的日期跟存檔裡的
  不一樣,今天的次數就是 0,不需要任何人去重設它。
- **日期對不上就一律歸零,包含「存檔比今天新」。** 玩家把系統時鐘往回調的時候,
  存檔的日期會比今天大。「只有變新才歸零」的寫法會讓次數**卡在用完的狀態回不來**,
  而那是修不好的存檔;「不一樣就歸零」則最多讓他多打幾輪。單機、沒有付費、沒有排行榜,
  多打幾輪是可以接受的代價,卡死不是。
- **日界線用本地午夜,不用 UTC。** UTC 會讓亞洲玩家在早上八點換日,那不是任何人會覺得是
  「新的一天」的時刻。算法是把本地的年月日取出來再當成 UTC 去算(`Date.UTC(y, m, d)`),
  得到的是「本地日曆天」的序號——跨時區搬家不會憑空多一天或少一天。
- **輪替表要照相剋環走,而且畫面要照「還要幾天」排。** 環的順序讓玩家看到今天是土就知道
  明天是冰、後天是火,「我想練火還要等兩天」是他自己算得出來的話。但**畫面照 `ELEMENTS`
  的順序排的話這件事就沒了**——實際畫出來是「火1天 金3天 雷2天 冰今天」,他得自己重排
  一次才讀得懂。排序照 `daysUntilElement`,那一排才讀得成日曆。
- **通關才記次數,失敗不記。** 每日次數的單位是「通關」——打輸了還要扣一次的話,
  卡關的玩家會連試都不敢試,而副本本來就是給他用的。入場費照收(那是門票),
  但次數不扣。
- **次數要守在資料那一層,不能只靠畫面把按鈕變灰。** 畫面的判斷跟真正的扣除是兩份程式,
  而「按鈕看起來能點但其實不該點」是最難查的一種 bug——何況存檔本來就是玩家改得動的。
- **「這一場拿到多少」不能透過 setState 傳給同一個 tick 的結果訊息。** state 要下一次
  render 才更新,所以結算那一刻讀到的是**上一場**的數字。症狀是「每次結算都顯示上一場的
  數量」,而且它看起來只是偶爾對不上,很難聯想到時序。掉落函式要**回傳**這一場的結果。
- **獎勵可以隨機,難度不行。** 副本一次給 5~15 本是刻意的(五場下來才有「今天手氣不錯」),
  而它安全的理由跟幅度無關:技能書與圖鑑只放大元素,而元素在理想路線之外,所以它動的
  只有獎勵,一格難度都沒碰到。對比之下閘門的爆發格就**不准**這樣抽(見 `DOUBLE_GATES_PER_RUN`:
  那個會讓玩家覺得「這場運氣好」而不是「我選得好」)。
- **逐屬性的抽落不能用「全體抽了再過濾」。** `rollDrops` 是在整個 5668 件的圈上找下一個
  沒收到的,加上屬性過濾之後會退化成「大部分位置都不合格」——收到後期要繞幾千步才找得到
  一件,而且屬性越冷門繞得越久。`rollDropsForElement` 先把該屬性的碎片攤成候選清單再洗牌,
  步數只跟那個屬性的件數有關。
- **單一等級換成逐屬性,遷移要「六個都給那一級」,不是除以六。** 舊制的那一級放大的是
  **全部**元素,所以等值的換算就是六個都是那一級。除以六的話老玩家一升版每個元素都掉一大截,
  而他完全不知道發生了什麼——這款在 `bestSurvival`(關→波)與技能書上限(5→100)
  各踩過一次同型的坑。

**存檔版號**
- **一個版號只能代表一件事。** 兩條分支同時把 `SAVE_VERSION` 從 4 升到 5(一條是技能書
  上限開到 100 級的等級換算,一條是任務欄位),合併的時候如果貪圖方便把兩件事塞進同一個 v5,
  遷移鏈就再也分不出「這份 v5 有沒有跑過技能書換算」——而**那一步不可重入**:
  跑兩次會把 5 級變成 100 級(`rescale(rescale(5))` = `rescale(37)` = 100)。
  已經上線的版號尤其不能重新定義:玩家硬碟上真的有那個版本的存檔。
  正解是版號往上再跳一格(v6),各自一條遷移。`verify-save` 有兩項在盯:
  v5→v6 技能書等級原封不動、v4→v6 剛好換算一次。

**存檔版號**
- **一個版號只能代表一件事。** 兩條分支同時把 `SAVE_VERSION` 從 4 升到 5(一條是技能書
  上限開到 100 級的等級換算,一條是任務欄位),合併的時候如果貪圖方便把兩件事塞進同一個 v5,
  遷移鏈就再也分不出「這份 v5 有沒有跑過技能書換算」——而**那一步不可重入**:
  跑兩次會把 5 級變成 100 級(`rescale(rescale(5))` = `rescale(37)` = 100)。
  **已經上線的版號尤其不能重新定義**:玩家硬碟上真的有那個版本的存檔。
  正解是版號往上再跳一格,每件事各一條遷移。**這條規則在這個專案短時間內救過三次**
  (v5 技能書上限 / v6 任務 / v7 永久技能移除 / v8 逐屬性技能書,四條線各自撞過一次)。`verify-save` 有兩項在盯:
  v5→v6 技能書等級原封不動、v4→v6 剛好換算一次。
- **副本的獎勵要跟「一般通關給什麼」一起看。** 技能書副本原本是「通關必得 1 本」,
  而同一時間另一條線把一般通關也改成保證給一本——那個副本當場失去存在理由
  (跟再打一關一模一樣,還要多付入場費)。改成 3 本、而且**不推進進度**:
  用金幣換技能書,代價是原地打轉。**新增任何獎勵型的入口,都要先問「不去那裡的人拿得到嗎」。**

**靜態輸出(web.output: static)**
- **`expo-audio` 的播放器不能在伺服器端建構,而它壞掉的樣子只有一行 `React #419`。**
  `web.output` 是 `static`,所以建置時 Expo 會在 **Node 裡**先把畫面渲染成 HTML;而
  `useBgm` 呼叫的 `useAudioPlayer` 在**建構的當下**就會做一個 media element
  (`AudioPlayerWeb` 的 constructor → `_createMediaElement()`),Node 裡沒有 `document`
  也沒有 `Audio`,那一下就丟例外。整個 Suspense boundary 在伺服器端失敗,匯出的
  `index.html` 會留下 **`<!--$!-->`**(React 的「這個 boundary 壞了」標記),
  瀏覽器接手時印一行壓縮過的 **React #419**。
  **最麻煩的是它看起來沒事**:React 會退回純用戶端渲染、自己重畫一次,畫面完全正常,
  只有 console 多一行錯誤碼。代價是預先渲染的 HTML 整段作廢(首屏白等一輪),
  而且**真正的錯誤訊息被 `<!--$!-->` 吃掉了**——以後任何在 SSR 階段壞掉的東西
  都會長成同一行 #419,查起來完全沒有線索。
  解法不是去 try/catch 那個播放器,是**根本不要在伺服器上畫遊戲**:`HomeScreen` 現在只做
  「掛載了沒」,沒掛載就畫 `Loading`,掛載後才畫 `<Game />`。伺服器輸出的是 Loading、
  瀏覽器第一次 render 畫的也是 Loading,兩邊一致所以 hydration 對得起來。
  這跟既有的 `loaded` 旗標是同一條規則的兩半:那條擋「存檔還沒讀完就開始玩」,
  這條擋「還沒進到瀏覽器就開始畫」,而且兩個畫的是同一個 Loading,不會閃兩次。
  **驗收方式是看匯出的 HTML 有沒有 `<!--$!-->`**,不是看畫面——畫面永遠是好的。

**版面(續)**
- **`PixelFrame` 的內距要走 `padding` prop,不能寫在 `style` 裡。** `style` 給的是外框那一層,
  而內容另有自己的 `padding`(預設 20),兩層會疊起來——卡片高度直接多一倍。
  同理**行距的 `gap` 也不能放在 `style`**:那一層除了內容還有八張裝飾圖(四角四邊),
  gap 會連裝飾一起排,框整個散開。要 gap 就在裡面再包一層 View。
- **鎖著的東西不要標紅價錢。** 副本卡片一開始把「錢夠不夠」跟「解鎖了沒」混在一起判斷,
  結果鎖著的副本入場費是紅的——玩家會以為問題出在錢,但他再有錢也進不去。

## 圖示鐵則:一律不用 emoji

畫面上任何位置的圖示都不能用 emoji,包含「先佔位之後再換」。理由:emoji 字體逐平台不同、
全彩高飽和跟本作的莫蘭迪暗色像素風格衝突、而且不是我們能調整的資產。

| 需求 | 做法 |
|---|---|
| 16x16 小圖示 | 從 `assets/sprites/ui/` 挑既有的 PNG(分頁圖示、鎖頭、金幣、齒輪都已經有) |
| 技能圖示 | 手寫 SVG 字形,見 `components/SkillIcon.tsx` 的 `GLYPHS`(圓形 + 冷卻環) |
| 面板外框、進度環、大尺寸橫幅 | 手寫 SVG + `react-native-svg` |

純排版符號 `✕`(關閉)、`✓`(已完成)、`→`(前往)不算 emoji,但不要擴大使用。

## 調色盤(沿用姊妹作,兩款視覺一致)

```
前景/文字   #f2f2f2   次要文字 #8a8a95
面板底      #2a2a35   面板亮階 #3a3448
強調金      #e0a95c   莫蘭迪紫 #9691a5
生命綠      #5ec26a   危險紅   #e05050
畫面底      #16161c
```

## 開發指令

- `npx expo start --web` — 啟動開發伺服器
- `npx tsc --noEmit` — 型別檢查
- `npx tsx scripts/verify-lane-run.ts` — 跑道數值驗證
- `npx tsx scripts/verify-lane-jobs.ts` — 轉職驗證(含「養成買不到勝利」的過關率對照)
- `npx tsx scripts/verify-lane-skills.ts` — 技能書與圖鑑驗證(放大得到元素,但碰不到理想路線)
- `npx tsx scripts/verify-save.ts` — 存檔驗證(壞掉/改過/舊版存檔都要進得去)
- `npx tsx scripts/verify-lane-tutorial.ts` — 教學關驗證(機制只增不減、教學區以外不受影響)
- `python3 scripts/align-frames.py` — 重新產生敵人的兩格動畫(換素材才要跑)

## 部署

線上位址:**https://gotbemate-del.github.io/daydreaming-swipe/**

push 到 `main` 會自動觸發 `.github/workflows/deploy-web.yml` 部署到 GitHub Pages。
Settings → Pages 的 Source 已經設成「GitHub Actions」(這一步只需要做一次,已完成)。

**開發分支上的 commit 不會部署**——工作流程只在 push 到 `main` 時觸發。改完東西之後
線上還是舊版是正常的,要合併到 `main` 才會上線。

**改完並且驗證通過之後就直接部署,不用問。**(合併到 `main` → 等 workflow 跑完 → 回報 sha)
驗證沒全過就不要部署,先回報是哪一項紅的。

一個排查用的事實:如果 Actions 分頁裡一次執行紀錄都沒有,不是工作流程壞了,
先確認 Pages 的 Source 是不是「GitHub Actions」——沒設定的話 deploy 那一步會回 404
(`Creating Pages deployment failed / HttpError: Not Found`),而 build 那一步是成功的,
很容易誤判成「建置有問題」。
